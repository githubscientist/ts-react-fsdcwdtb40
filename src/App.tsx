const App = () => {
  // data & js functions
  let name: string = 'krish';

  return (
    <>
      <h1>Hello { name.slice(0, 1).toUpperCase() + name.slice(1, ) }!</h1>
    </>
  )
}

export default App;